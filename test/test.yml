# Standards: 0.1
---
- hosts: cassandra

  remote_user: root

  pre_tasks:
  - name: Fedora Workaround
    set_fact:
      cassandra_systemd_enabled: True
    when: ansible_facts['distribution'] == 'Fedora'

  roles:
  - cassandra
